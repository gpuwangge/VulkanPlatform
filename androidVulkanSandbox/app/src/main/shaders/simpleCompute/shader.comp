#version 450 core

layout (local_size_x = 4, local_size_y = 1, local_size_z = 1) in; //workgroup size

layout (set = 0, binding = 0) buffer Buffer{
     uint value[4];
} buf;

void main(){
    buf.value[0] = gl_NumWorkGroups.x;//gl_NumWorkGroups is special value. Input of the dispatch function.
    buf.value[1] = gl_NumWorkGroups.y;
    buf.value[2] = gl_NumWorkGroups.z;
}


