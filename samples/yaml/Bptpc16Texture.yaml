Object:
  Models: [CUSTOM3D0]
  Textures: [48bpt.png]
  TextureMipmaps: [true]
  TextureCubemap: [false]
  VertexShaders: [bptpc16Texture/shader.vert.spv]
  FragmentShaders: [bptpc16Texture/shader.frag.spv]
  ComputeShaders: []
  
  ModelList: [0]
  TextureList: [0]
  GraphicsPipelineList: [0]

Uniform:
  # Graphcis: CustomGraphics_MVP_VP (assume all graphics pipelines use the same descriptor uniform layout/set)
  Graphics: [[false, true, false]]
  # Sampler: SamplersNumber
  Sampler: [1]
  # Compute: CustomCompute_Storage_SwapchainStorage_TextureStorage (assume all compute pipelines use the same descriptor uniform layout/set)
  Compute: []

Feature:
  GraphicsDepthTest: false
  GraphicsMSAA: true
  Graphics48BPT: true
  GraphicsPushConstant: false
  GraphicsBlend: false
  GraphicsRainbowMipmap: false
  GraphicsPipelineSkyboxID: -1

MainCamera:
  FreeMode: false
  Position: [0,0,-3]
  Rotation: [0,0,0]
  TargetLocation: [0,0,0]
  FOV: 90
  Z: [0.1, 256]

