Object:
  Models: [ElegantChairTable.obj]
  Textures: [repeat-pattern2.jpg]
  TextureMipmaps: [true]
  TextureCubemap: [false]
  VertexShaders: [simpleObjTransform/shader.vert.spv]
  FragmentShaders: [simpleObjTransform/shader.frag.spv]
  ComputeShaders: []
  
  ModelList: [0]
  TextureList: [0]
  PipelineList: [0]

Uniform:
  # Graphcis: CustomGraphics_MVP_VP
  Graphics: [[false, true, false]]
  # Sampler: SamplersNumber
  Sampler: [1]
  # Compute: CustomCompute_Storage_SwapchainStorage_TextureStorage (for now assume sample only need one compute pipeline)
  Compute: []

Feature:
  GraphicsDepthTest: false
  GraphicsMSAA: true
  Graphics48BPT: false
  GraphicsPushConstant: false
  GraphicsBlend: false
  GraphicsRainbowMipmap: false
  GraphicsPipelineSkyboxID: -1

MainCamera:
  FreeMode: true
  Position: [0,1.2,0]
  Rotation: [0,0,0]
  TargetLocation: [0,0,0]
  FOV: 90
  Z: [0.01, 256]

